# Polymarket CLOB Arbitrage Bot - Env Example

# Polymarket API Credentials
# Private key for signing transactions (L1 signer)
# Obtain from MetaMask export or hardware wallet
POLYMARKET_PRIVATE_KEY=0x

# Polymarket API credentials (L2 - if required)
# These may be needed depending on client configuration
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# Telegram Notifications (optional)
# Get bot token from BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Get chat ID: message your bot then visit
# https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_CHAT_ID=

# Execution Mode
# Options: dry (detect only) | paper (simulate) | live (real execution)
MODE=dry

# Logging Level
# Options: debug | info | warn | error
LOG_LEVEL=info

# Kill Switch
# Set to 1 to immediately stop all trading and cancel orders
KILL_SWITCH=0
